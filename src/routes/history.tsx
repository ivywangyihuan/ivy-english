import { useEffect, useMemo, useState } from "react";
import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, CheckCircle2, Clock3, History, RotateCcw, XCircle } from "lucide-react";
import { Chip, PageHeader, SectionTitle } from "@/components/ui-kit";
import { useAppState } from "@/state/app-state";
import { getExamHistory, type ExamSession } from "@/lib/exam-engine-store";
import { keyFromLabel, labelFromKey, learningModules, moduleMeta, type LearningModuleKey } from "@/lib/learning-navigation";

export type HistoryModule = LearningModuleKey | "all";
export const Route=createFileRoute("/history")({validateSearch:(search:Record<string,unknown>):{module:HistoryModule}=>{const valid:HistoryModule[]=["all","listening","speaking","reading","writing"];return{module:valid.includes(search.module as HistoryModule)?search.module as HistoryModule:"all"}},head:()=>({meta:[{title:"学习历史 · Ivy English"}]}),component:HistoryPage});

function engineKey(module:ExamSession["module"]):LearningModuleKey{return module==="reading"?"reading":module==="listening"?"listening":module==="writing"?"writing":"speaking"}
function moduleLabel(module:ExamSession["module"]){return labelFromKey(engineKey(module))}
function retry(module:ExamSession["module"]){return moduleMeta(engineKey(module)).examHref}
function formatDuration(seconds:number){const total=Math.max(0,Math.round(seconds));const minutes=Math.floor(total/60);const rest=total%60;return `${minutes}:${rest.toString().padStart(2,"0")}`}

function EngineSessionCard({item}:{item:ExamSession}){const answered=item.answers.filter(answer=>answer.answer.trim()).length;const highlights=item.annotations?.filter(a=>a.type==="highlight")??[];const notes=item.annotations?.filter(a=>a.type==="note")??[];const vocabulary=item.vocabulary??item.annotations?.filter(a=>a.type==="vocabulary").map(a=>a.text)??[];return <article className="surface overflow-hidden"><div className="p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-[11px] text-muted-foreground"><span className="rounded-full bg-sage-soft px-2 py-0.5 text-sage-foreground">{moduleLabel(item.module)}</span><span>{item.mode==="full_exam"?"完整模拟":"专项练习"}</span></div><p className="mt-3 text-sm">{new Date(item.completedAt??item.startedAt).toLocaleString("zh-CN")}</p></div><div className="text-right"><p className="display text-xl tabular-nums">{typeof item.score==="number"?`${item.score} / ${item.answers.length}`:"Completed"}</p><p className="mt-1 inline-flex items-center gap-1 text-[11px] text-muted-foreground"><Clock3 className="size-3"/>{formatDuration(item.durationSeconds)}</p></div></div><div className="mt-4 flex flex-wrap gap-2 text-[11px] text-muted-foreground"><span>{answered} answered</span>{item.answers.length?<span>· {item.answers.length} items</span>:null}{highlights.length?<span>· {highlights.length} highlights</span>:null}{notes.length?<span>· {notes.length} notes</span>:null}{vocabulary.length?<span>· {vocabulary.length} vocabulary</span>:null}</div><a href={retry(item.module)} className="mt-4 inline-flex items-center gap-1.5 text-[11px] text-sage-foreground hover:underline"><RotateCcw className="size-3"/>再练一次</a></div>{item.answers.length?<details className="border-t border-border"><summary className="cursor-pointer px-5 py-3 text-xs text-muted-foreground hover:bg-secondary/35 sm:px-6">展开完整答题记录</summary><div className="max-h-[55vh] divide-y divide-border overflow-y-auto">{item.answers.map((answer,index)=><div key={`${answer.questionId}-${index}`} className="grid gap-2 px-5 py-4 text-xs sm:grid-cols-[42px_1fr_auto] sm:items-start sm:px-6"><span className="font-medium text-muted-foreground">{index+1}</span><div className="min-w-0"><p className="font-medium">{answer.questionId}</p><p className="mt-1 break-words text-muted-foreground">你的答案：{answer.answer.trim()||"未作答"}</p>{answer.correctAnswer?<p className="mt-1 break-words text-muted-foreground">正确答案：{answer.correctAnswer}</p>:null}{item.module==="writing"&&answer.answer?<p className="mt-3 max-h-72 overflow-y-auto whitespace-pre-wrap rounded-lg bg-secondary/45 p-3 leading-5">{answer.answer}</p>:null}</div>{typeof answer.isCorrect==="boolean"?<span className={answer.isCorrect?"inline-flex items-center gap-1 text-sage-foreground":"inline-flex items-center gap-1 text-amber-700"}>{answer.isCorrect?<CheckCircle2 className="size-3.5"/>:<XCircle className="size-3.5"/>}{answer.isCorrect?"正确":"错误"}</span>:null}</div>)}</div></details>:null}{highlights.length||notes.length||vocabulary.length?<details className="border-t border-border"><summary className="cursor-pointer px-5 py-3 text-xs text-muted-foreground hover:bg-secondary/35 sm:px-6">展开标注与生词</summary><div className="grid gap-5 px-5 pb-5 pt-2 text-xs sm:px-6 md:grid-cols-3">{highlights.length?<div><p className="font-medium">Highlights</p><ul className="mt-2 space-y-2">{highlights.map(annotation=><li key={annotation.id} className="rounded-lg bg-[#fff8bf] px-3 py-2">{annotation.text}</li>)}</ul></div>:null}{notes.length?<div><p className="font-medium">Notes</p><ul className="mt-2 space-y-2 text-muted-foreground">{notes.map(annotation=><li key={annotation.id} className="whitespace-pre-wrap rounded-lg bg-secondary/45 px-3 py-2">{annotation.text}</li>)}</ul></div>:null}{vocabulary.length?<div><p className="font-medium">Vocabulary</p><div className="mt-2 flex flex-wrap gap-2">{vocabulary.map((word,index)=><span key={`${word}-${index}`} className="rounded-full bg-sage-soft px-2.5 py-1 text-sage-foreground">{word}</span>)}</div></div>:null}</div></details>:null}</article>}

function HistoryPage(){const {module}=Route.useSearch();const {sessions}=useAppState();const [examHistory,setExamHistory]=useState<ExamSession[]>([]);useEffect(()=>setExamHistory(getExamHistory().filter(item=>item.status==="completed")),[]);const currentLabel=module==="all"?null:labelFromKey(module);const recent=useMemo(()=>sessions.filter(item=>!currentLabel||item.module===currentLabel),[currentLabel,sessions]);const engine=useMemo(()=>examHistory.filter(item=>module==="all"||engineKey(item.module)===module),[examHistory,module]);const title=module==="all"?"全部学习历史":`${currentLabel}历史`;const subtitle=module==="writing"?"完整查看写过的作文、题目、字数和练习来源。":module==="speaking"?"回看做过的话题、时长与录音入口。":module==="listening"?"回看听过的材料、听写、CBT 答题与复盘。":module==="reading"?"回看文章、笔记、CBT 答题、标注和生词。":"所有听说读写记录都从这里进入，不再绕回进度页。";
return <div className="space-y-9"><div className="flex flex-wrap items-center justify-between gap-3"><Link to={module==="all"?"/progress":"/practice"} search={module==="all"?undefined:{module}} className="inline-flex items-center gap-1.5 text-xs text-muted-foreground hover:text-foreground"><ArrowLeft className="size-3.5"/>{module==="all"?"返回进度":"返回学习"}</Link>{module!=="all"?<Link to="/progress-detail" search={{module}} className="text-xs text-sage-foreground hover:underline">查看{currentLabel}进度</Link>:null}</div><PageHeader title={title} subtitle={subtitle}/><div className="flex gap-2 overflow-x-auto pb-1"><Chip active={module==="all"}><Link to="/history" search={{module:"all"}}>全部</Link></Chip>{learningModules.map(meta=><Chip key={meta.key} active={module===meta.key}><Link to="/history" search={{module:meta.key}}>{meta.label}</Link></Chip>)}</div>
{engine.length?<section><SectionTitle title="IELTS / CBT 记录" subtitle={`${engine.length} 场`}/><div className="space-y-3">{engine.map(item=><EngineSessionCard key={item.id} item={item}/>)}</div></section>:null}
<section><SectionTitle title={module==="all"?"全部学习记录":`${currentLabel}学习记录`} subtitle={`${recent.length} 条`}/><div className="surface divide-y divide-border overflow-hidden">{recent.map(item=>{const key=keyFromLabel(item.module);return <Link key={item.id} to="/session" search={{id:item.id,from:"history",module:key}} className="flex items-center justify-between gap-4 px-5 py-4 hover:bg-secondary/40"><div className="min-w-0"><p className="truncate text-sm">{item.activity}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.date} · {item.module} · {item.durationMinutes} min{item.tool?` · ${item.tool}`:""}</p></div><span className="shrink-0 text-xs tabular-nums">{item.score??"查看详情"}</span></Link>})}{!recent.length?<div className="py-14 text-center"><History className="mx-auto size-7 text-sage"/><p className="mt-4 text-sm">这个模块还没有学习记录。</p></div>:null}</div></section></div>}
