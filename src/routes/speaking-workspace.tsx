import { useEffect, useMemo, useRef, useState } from "react";
import { createFileRoute, Link } from "@tanstack/react-router";
import { ChevronLeft, Mic, Pause, Play, RotateCcw, Square, Volume2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useAppState } from "@/state/app-state";
import { cn } from "@/lib/utils";

type Mode="free"|"minute"|"retell"|"image"|"today"|"part1"|"part2"|"part3"|"full";
export const Route=createFileRoute("/speaking-workspace")({validateSearch:(search:Record<string,unknown>):{mode:Mode}=>{const modes:Mode[]=["free","minute","retell","image","today","part1","part2","part3","full"];return{mode:modes.includes(search.mode as Mode)?search.mode as Mode:"free"}},head:()=>({meta:[{title:"Speaking Workspace · Ivy English"}]}),component:SpeakingWorkspace});

const configs:Record<Mode,{title:string;subtitle:string;prompts:string[];answerSeconds?:number;prepSeconds?:number}>={
  free:{title:"自由表达",subtitle:"想到什么就说，不需要像考试。",prompts:["What is on your mind today?"]},
  minute:{title:"一分钟话题",subtitle:"用一分钟把一个小观点说完整。",prompts:["What is one small thing that made your week better?"],answerSeconds:60},
  retell:{title:"复述练习",subtitle:"先读一遍素材，再用自己的话讲出来。",prompts:["A student missed an early train, changed her route, and unexpectedly found a quiet café where she finished an assignment. Retell the story in your own words."],answerSeconds:90},
  image:{title:"图片描述",subtitle:"先观察，再按整体—细节—推测来描述。",prompts:["Imagine a busy city square on a rainy evening. Describe what you can see, what people may be doing, and how the scene feels."],answerSeconds:90},
  today:{title:"今日话题",subtitle:"每天一个自然表达题。",prompts:["Would you rather have a very planned week or leave more room for spontaneous decisions? Why?"]},
  part1:{title:"IELTS Speaking · Part 1",subtitle:"短答自然展开，不背答案。",prompts:["Do you enjoy the place where you live?","What do you usually do at weekends?","Do you often change your plans?"] ,answerSeconds:45},
  part2:{title:"IELTS Speaking · Part 2",subtitle:"1 分钟准备，最多 2 分钟回答。",prompts:["Describe a place you would like to visit again. You should say where it is, when you went there, what you did there, and explain why you would like to return."],prepSeconds:60,answerSeconds:120},
  part3:{title:"IELTS Speaking · Part 3",subtitle:"观点—原因—例子，让回答自然展开。",prompts:["Why do some people enjoy returning to the same places?","How has travel changed in recent years?","Can travelling help people understand themselves better?"],answerSeconds:60},
  full:{title:"IELTS Speaking · Full Simulation",subtitle:"Part 1 → Part 2 → Part 3，连续完成。",prompts:["Part 1 · What do you normally do on weekends?","Part 1 · Do you like making plans in advance?","Part 2 · Describe a place you would like to visit again.","Part 3 · Why do people become attached to certain places?","Part 3 · Is travel more valuable for young people than older people?"],answerSeconds:60},
};

function format(seconds:number){const safe=Math.max(0,Math.floor(seconds));return `${Math.floor(safe/60).toString().padStart(2,"0")}:${(safe%60).toString().padStart(2,"0")}`;}

function SpeakingWorkspace(){
  const {mode}=Route.useSearch();const config=configs[mode];const {addSession}=useAppState();
  const [promptIndex,setPromptIndex]=useState(0);const [phase,setPhase]=useState<"idle"|"prep"|"answer"|"done">("idle");const [seconds,setSeconds]=useState(0);const [running,setRunning]=useState(false);const [recording,setRecording]=useState(false);const [audioUrl,setAudioUrl]=useState<string|null>(null);const [permissionError,setPermissionError]=useState("");
  const recorderRef=useRef<MediaRecorder|null>(null);const streamRef=useRef<MediaStream|null>(null);const chunksRef=useRef<Blob[]>([]);const startedAtRef=useRef<number|null>(null);const totalSecondsRef=useRef(0);
  const prompt=config.prompts[promptIndex]??config.prompts[0]!;
  const isTimed=Boolean(config.answerSeconds);

  useEffect(()=>{if(!running)return;const timer=window.setInterval(()=>setSeconds(value=>{if((phase==="prep"||phase==="answer")&&isTimed){if(value<=1){if(phase==="prep"){setPhase("answer");setSeconds(config.answerSeconds??60);startRecorder();return config.answerSeconds??60;}setRunning(false);setPhase("done");stopRecorder();return 0;}return value-1;}return value+1;}),1000);return()=>window.clearInterval(timer)},[running,phase,isTimed,config.answerSeconds]);
  useEffect(()=>()=>{streamRef.current?.getTracks().forEach(track=>track.stop());if(audioUrl)URL.revokeObjectURL(audioUrl)},[audioUrl]);

  async function ensureStream(){if(streamRef.current)return streamRef.current;try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});streamRef.current=stream;setPermissionError("");return stream;}catch{setPermissionError("没有获得麦克风权限。你仍然可以用计时器练习，但这一轮不会保存录音。");return null;}}
  async function startRecorder(){const stream=await ensureStream();if(!stream||recorderRef.current?.state==="recording")return;chunksRef.current=[];const recorder=new MediaRecorder(stream);recorderRef.current=recorder;recorder.ondataavailable=e=>{if(e.data.size)chunksRef.current.push(e.data)};recorder.onstop=()=>{if(!chunksRef.current.length)return;const blob=new Blob(chunksRef.current,{type:recorder.mimeType||"audio/webm"});if(audioUrl)URL.revokeObjectURL(audioUrl);setAudioUrl(URL.createObjectURL(blob));};recorder.start();setRecording(true);startedAtRef.current=Date.now();}
  function stopRecorder(){const recorder=recorderRef.current;if(recorder&&recorder.state!=="inactive")recorder.stop();if(startedAtRef.current){totalSecondsRef.current+=Math.max(0,Math.round((Date.now()-startedAtRef.current)/1000));startedAtRef.current=null;}setRecording(false);}
  async function begin(){await ensureStream();setAudioUrl(null);if(config.prepSeconds){setPhase("prep");setSeconds(config.prepSeconds);setRunning(true);return;}setPhase("answer");setSeconds(config.answerSeconds??0);setRunning(true);await startRecorder();}
  function pause(){setRunning(false);if(recorderRef.current?.state==="recording"){recorderRef.current.pause();setRecording(false);}}
  function resume(){setRunning(true);if(recorderRef.current?.state==="paused"){recorderRef.current.resume();setRecording(true);}}
  function reset(){setRunning(false);stopRecorder();setPhase("idle");setSeconds(0);setAudioUrl(null);}
  function next(){stopRecorder();setRunning(false);setPhase("idle");setSeconds(0);setAudioUrl(null);setPromptIndex(i=>Math.min(config.prompts.length-1,i+1));}
  function finish(){stopRecorder();const duration=Math.max(1,Math.round((totalSecondsRef.current+(startedAtRef.current?Math.max(0,(Date.now()-startedAtRef.current)/1000):0))/60));addSession({date:new Date().toISOString().slice(0,10),module:"口语",activity:config.title,tool:"Ivy Speaking Workspace",durationMinutes:duration,notes:`完成 ${promptIndex+1}/${config.prompts.length} 个话题`});setPhase("done");setRunning(false);}

  return <div className="min-h-screen bg-[#f4f1ea] text-[#262a27]"><div className="mx-auto flex min-h-screen max-w-5xl flex-col px-5 py-6 sm:px-8"><header className="flex items-center justify-between"><Link to="/practice" search={{module:"speaking"}} className="inline-flex items-center gap-1.5 text-xs text-[#66706a]"><ChevronLeft className="size-4"/>返回学习</Link><span className="text-[11px] text-[#7a817d]">Speaking Workspace</span></header><main className="grid flex-1 items-center gap-8 py-8 lg:grid-cols-[0.8fr_1.2fr]"><section><div className="flex size-16 items-center justify-center rounded-full bg-[#dfe8df] text-[#47634e]"><Volume2 className="size-7"/></div><p className="mt-6 text-xs uppercase tracking-[0.18em] text-[#758079]">Examiner / Prompt</p><h1 className="mt-2 font-serif text-3xl leading-tight">{config.title}</h1><p className="mt-2 text-sm leading-6 text-[#687069]">{config.subtitle}</p><div className="mt-8 rounded-2xl border border-[#d6d4cc] bg-white/70 p-5 shadow-sm"><p className="text-[11px] text-[#7d817d]">{config.prompts.length>1?`Question ${promptIndex+1} / ${config.prompts.length}`:"Your prompt"}</p><p className="mt-3 text-lg leading-8">{prompt}</p></div>{permissionError?<p className="mt-4 rounded-xl bg-[#fff0dd] p-3 text-xs leading-5 text-[#855b20]">{permissionError}</p>:null}</section><section className="rounded-3xl border border-[#d8d5cc] bg-white p-6 shadow-sm sm:p-8"><div className="flex items-start justify-between"><div><p className="text-xs text-[#777e79]">{phase==="prep"?"Preparation":phase==="answer"?"Answer":phase==="done"?"Completed":"Ready"}</p><p className="mt-2 font-mono text-5xl tabular-nums">{format(seconds)}</p></div><span className={cn("rounded-full px-3 py-1 text-[10px]",recording?"bg-[#f6dfdc] text-[#8c4037]":"bg-[#edf0ec] text-[#66706a]")}>{recording?"RECORDING":"MIC READY"}</span></div><div className="mt-8 flex flex-wrap items-center gap-3">{phase==="idle"?<Button onClick={begin} className="rounded-full px-5"><Mic className="mr-2 size-4"/>{config.prepSeconds?"开始准备":"开始回答"}</Button>:null}{running?<Button variant="outline" onClick={pause} className="rounded-full"><Pause className="mr-2 size-4"/>暂停</Button>:phase!=="idle"&&phase!=="done"?<Button onClick={resume} className="rounded-full"><Play className="mr-2 size-4"/>继续</Button>:null}<Button variant="outline" onClick={reset} className="rounded-full"><RotateCcw className="mr-2 size-4"/>重置</Button>{phase!=="idle"?<Button variant="outline" onClick={finish} className="rounded-full"><Square className="mr-2 size-3.5"/>结束本轮</Button>:null}</div>{audioUrl?<div className="mt-7 rounded-2xl bg-[#f5f5f1] p-4"><p className="mb-2 text-xs text-[#6c746e]">刚刚的录音</p><audio controls src={audioUrl} className="w-full"/></div>:null}{config.prompts.length>1?<div className="mt-7 flex items-center justify-between border-t border-[#e3e1da] pt-5"><span className="text-xs text-[#777e79]">{promptIndex+1} / {config.prompts.length}</span><button type="button" disabled={promptIndex>=config.prompts.length-1} onClick={next} className="rounded-full border border-[#cfcfc8] px-4 py-2 text-xs disabled:opacity-40">下一题</button></div>:null}</section></main></div></div>;
}
